#!/usr/bin/env bash
# Tracked pre-commit hook. Enable once per clone with `make git-hooks`.
# Runs the same gate CI runs. A commit that fails here would fail there.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
make -s check
